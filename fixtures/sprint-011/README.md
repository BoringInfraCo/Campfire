# Sprint 011 — Webhook delivery delays

Workspace id: provided by the harness at runtime, not hardcoded here.

Work request: investigate delayed webhook deliveries using `webhook.log`
and `queue-config.json` in this directory, then write your diagnosis
to `./diagnosis.md`.

Access: you can read the files in this directory and use the ordinary
Campfire tools available in this workspace.

Keep durable shared state current as you work.
